[tool.poetry]
name = "patterns"
version = "0.1.0"
description = "isu patterns"
authors = ["Veronika"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
